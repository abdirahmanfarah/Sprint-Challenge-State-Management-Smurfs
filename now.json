{
  "version": 2,
  "name": "smurfs",
  "builds": [{ "src": "smurfs/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
  "routes": [
      { "src": "/static/(.*)", "dest": "smurfs/static/$1" }
  ]
}